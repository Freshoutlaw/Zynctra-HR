apiVersion: apps/v1
kind: Deployment
metadata:
  name: connectors
spec:
  replicas: 2
  selector:
    matchLabels:
      app: connectors
  template:
    metadata:
      labels:
        app: connectors
    spec:
      containers:
      - name: connectors
        image: zynctra/connectors:latest
